version: '3.8'

services:
  dashboard-financeiro:
    build: .
    container_name: dashboard-financeiro
    restart: unless-stopped
    ports:
      - "5001:80"
    environment:
      - TZ=America/Sao_Paulo
